x1=[1 3 0 4]
y1=[1 2 3 0]
y=xcorr(x1,y1)
subplot(3,1,1);
stem(x1);
xlabel('discrete in time');
ylabel('amp');
title('inputseq of x1');
subplot(3,1,2);
stem(y1);
xlabel('discrete in time');
ylabel('amp');
title('inputseq of y1');
stem(y);
title('crosscorrelation');
xlabel('discrete time');
ylabel('amp');